public class Bus extends Transport{

    public Bus(String brand, String model, String color, int productionYear, String origin, int maxSpeed) {
        super(brand, model, color, productionYear, origin, maxSpeed);
    }
}
